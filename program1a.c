#include <stdio.h>

int main() {
    int number; // Declare an integer variable

    printf("Enter an integer: "); // Prompt the user for input
    scanf("%d", &number); // Read an integer from the user and store it in 'number'

    printf("You entered: %d\n", number); // Print the entered number

    return 0;
}
